[1mdiff --git a/Proyecto/python.py b/Proyecto/python.py[m
[1mindex d76b4cd..dfec39c 100644[m
[1m--- a/Proyecto/python.py[m
[1m+++ b/Proyecto/python.py[m
[36m@@ -211,7 +211,9 @@[m [mdef mostrar_interfaz_temp_hum():[m
             tmax = float(temp_entry.get())[m
             hmax = float(hum_entry.get())[m
             if mySerial:[m
[31m-                mySerial.write(f"12:{tmax}:{hmax}\n".encode())[m
[32m+[m[32m                mensaje = f"12:{tmax}:{hmax}"[m
[32m+[m[32m                checksum = hacerChecksum(mensaje)[m
[32m+[m[32m                mySerial.write(f"{mensaje}|{checksum}\n".encode())[m
                 print("Valores enviados al satélite:",[m
                       f"12:{tmax}:{hmax}\n".strip())[m
         except:[m
[36m@@ -320,9 +322,10 @@[m [mdef mostrar_interfaz_radar():[m
         if mySerial:[m
             ang = angulo_entry.get().strip()[m
             if ang.isdigit():[m
[31m-                cmd = f"8:{ang}\n".encode()[m
[31m-                mySerial.write(cmd)[m
[31m-                print("Ángulo fijo enviado:", cmd)[m
[32m+[m[32m                mensaje = f"8:{ang}"[m
[32m+[m[32m                checksum = hacerChecksum(mensaje)[m
[32m+[m[32m                mySerial.write(f"{mensaje}|{checksum}\n".encode())[m
[32m+[m[32m                print("Ángulo fijo enviado:", mensaje)[m
 [m
     Button(window, text="Aplicar Ángulo", bg='lightblue',[m
            command=aplicar_angulo).grid(row=3, column=3)[m
[36m@@ -453,13 +456,13 @@[m [mdef enviar_nuevo_periodo_datos_temp_hum():[m
     """[m
     Coge el valor que escribió el usuario en la caja[m
     y se lo manda al satélite como '5:valor'.[m
[31m-    Este comando NO lleva checksum porque el Arduino[m
[31m-    no lo exige para los mensajes que recibe.[m
     """[m
     try:[m
         val = int(periodo_TH_entry.get())[m
         if mySerial:[m
[31m-            mySerial.write(f"5:{val}\n".encode())[m
[32m+[m[32m            mensaje = f"5:{val}"[m
[32m+[m[32m            checksum = hacerChecksum(mensaje)[m
[32m+[m[32m            mySerial.write(f"{mensaje}|{checksum}\n".encode())[m
     except:[m
         print("Valor incorrecto periodo TH")[m
         reproducir_fallo()[m
[36m@@ -472,7 +475,9 @@[m [mdef enviar_nuevo_periodo_datos_dist():[m
     try:[m
         val = int(periodo_D_entry.get())[m
         if mySerial:[m
[31m-            mySerial.write(f"6:{val}\n".encode())[m
[32m+[m[32m            mensaje = f"6:{val}"[m
[32m+[m[32m            checksum = hacerChecksum(mensaje)[m
[32m+[m[32m            mySerial.write(f"{mensaje}|{checksum}\n".encode())[m
     except:[m
         print("Valor incorrecto periodo Dist")[m
         reproducir_fallo()[m
[36m@@ -492,12 +497,11 @@[m [mdef hacer_medias_satelite():[m
 def hacer_medias_tierra():[m
     """[m
     El usuario decide que las medias las calcule el PC aquí.[m
[31m-    Esto NO requiere checksum.[m
     """[m
     global medias_tierra[m
     medias_tierra = True[m
     if mySerial:[m
[31m-        mySerial.write(b"11:\n")[m
[32m+[m[32m        mySerial.write(b"11:|156\n")[m
 [m
 [m
 # ======================================================[m
[36m@@ -537,26 +541,44 @@[m [mdef leer_datos_serial():[m
 [m
         linea = mySerial.readline().decode('utf-8', errors='ignore').strip()[m
 [m
[31m-        if linea:[m
[31m-            # Separar mensaje y checksum[m
[31m-            trozosCheck = linea.split('|')[m
[32m+[m[32m        # Si viene vacío, no hacemos nada[m
[32m+[m[32m        if not linea:[m
[32m+[m[32m            return None[m
 [m
[31m-            # El mensaje antes del '|'[m
[31m-            mensaje_sin_checksum = trozosCheck[0][m
[31m-            checksum_aqui = hacerChecksum(mensaje_sin_checksum)[m
[31m-            checksum_mensaje = trozosCheck[1][m
[32m+[m[32m        # --- Separar mensaje y checksum ---[m
[32m+[m[32m        trozosCheck = linea.split('|')[m
 [m
[31m-            # Comprobamos integridad[m
[31m-            if checksum_aqui != int(checksum_mensaje):[m
[31m-                print("El checksum no coincide en el mensaje:", linea)[m
[31m-                return None[m
[32m+[m[32m        # Si NO hay checksum, descartamos la línea[m
[32m+[m[32m        if len(trozosCheck) < 2:[m
[32m+[m[32m            print("Mensaje sin checksum (ignorando):", linea)[m
[32m+[m[32m            return None[m
[32m+[m
[32m+[m[32m        mensaje_sin_checksum = trozosCheck[0][m
[32m+[m[32m        str_checksum = trozosCheck[1].strip()[m
[32m+[m
[32m+[m[32m        # Si el checksum no es numérico, descartamos[m
[32m+[m[32m        if not str_checksum.isdigit():[m
[32m+[m[32m            print("Checksum no numérico (ignorando):", linea)[m
[32m+[m[32m            return None[m
[32m+[m
[32m+[m[32m        checksum_mensaje = int(str_checksum)[m
[32m+[m[32m        checksum_aqui = hacerChecksum(mensaje_sin_checksum)[m
[32m+[m
[32m+[m[32m        # Comprobamos integridad[m
[32m+[m[32m        if checksum_aqui != checksum_mensaje:[m
[32m+[m[32m            print("El checksum no coincide en el mensaje:", linea)[m
[32m+[m[32m            return None[m
 [m
[31m-            # Ahora analizamos el mensaje real[m
[31m-            trozos = mensaje_sin_checksum.split(":")[m
[31m-            codigo = trozos[0][m
[32m+[m[32m        # Ahora analizamos el mensaje real[m
[32m+[m[32m        trozos = mensaje_sin_checksum.split(":")[m
[32m+[m[32m        codigo = trozos[0][m
 [m
[31m-            # Rozando por casos[m
[32m+[m[32m        # Rozando por casos[m
[32m+[m[32m        try:[m
             if codigo == "1":  # temp/hum[m
[32m+[m[32m                if len(trozos) < 3:[m
[32m+[m[32m                    print("Mensaje 1: incompleto:", linea)[m
[32m+[m[32m                    return None[m
                 return codigo, float(trozos[1]), float(trozos[2])[m
 [m
             elif codigo == "2":  # error temp/hum[m
[36m@@ -565,6 +587,9 @@[m [mdef leer_datos_serial():[m
                 return None[m
 [m
             elif codigo == "3":  # dist/ang[m
[32m+[m[32m                if len(trozos) < 3:[m
[32m+[m[32m                    print("Mensaje 3: incompleto:", linea)[m
[32m+[m[32m                    return None[m
                 return codigo, float(trozos[1]), float(trozos[2])[m
 [m
             elif codigo == "4":[m
[36m@@ -573,6 +598,9 @@[m [mdef leer_datos_serial():[m
                 return None[m
 [m
             elif codigo == "5":  # medias[m
[32m+[m[32m                if len(trozos) < 3:[m
[32m+[m[32m                    print("Mensaje 5: incompleto:", linea)[m
[32m+[m[32m                    return None[m
                 return codigo, float(trozos[1]), float(trozos[2])[m
 [m
             elif codigo == "6":[m
[36m@@ -581,11 +609,25 @@[m [mdef leer_datos_serial():[m
                 return None[m
 [m
             elif codigo == "9":  # posición[m
[32m+[m[32m                if len(trozos) < 4:[m
[32m+[m[32m                    print("Mensaje 9: incompleto:", linea)[m
[32m+[m[32m                    return None[m
                 return codigo, float(trozos[1]), float(trozos[2]), float(trozos[3])[m
 [m
[32m+[m[32m            else:[m
[32m+[m[32m                # Código desconocido → lo ignoramos[m
[32m+[m[32m                print("Código desconocido (ignorando):", linea)[m
[32m+[m[32m                return None[m
[32m+[m
[32m+[m[32m        except ValueError:[m
[32m+[m[32m            # Algún float() ha fallado[m
[32m+[m[32m            print("Error parseando números en el mensaje:", linea)[m
[32m+[m[32m            return None[m
[32m+[m
     return None[m
[32m+[m
 # ======================================================[m
[31m-# ================ ACTUALIZACIÓN GENERAL ===============[m
[32m+[m[32m# ================ ACTUALIZACIÓN GvERAL ===============[m
 # ======================================================[m
 def actualizar_todo():[m
     """[m
